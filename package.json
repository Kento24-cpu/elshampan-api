{
  "name": "elshampan-api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API for El Shampan liquor store",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:setup": "node scripts/db-setup.js",
    "db:reset": "node scripts/db-setup.js --reset",
    "lint": "eslint .",
    "test": "NODE_ENV=test DB_NAME=elshampan_test node scripts/db-setup.js --reset --quiet && NODE_ENV=test DB_NAME=elshampan_test node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^18.0.0",
    "express": "^5.2.1",
    "mysql2": "^3.24.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "nodemon": "^3.1.14"
  }
}
