# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# MySQL / MariaDB
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=elshampan
DB_PASSWORD=
DB_NAME=elshampan

# Auth
SESSION_TTL_DAYS=7

# CORS: "*" allows every origin, or a comma separated allowlist
CORS_ORIGINS=*

# Throttle for /auth/login and /auth/register
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_LIMIT=20
